os: linux
dist: xenial
language: perl
perl:
  - "5.30"

before_install:
  - "sudo apt-get update"
  - "sudo apt-get install -y libdb-dev"
  - "git clone git://github.com/tjunier/newick_utils.git && cd newick_utils && autoreconf -fi && ./configure && make && sudo make install"
  - "cpanm --sudo -v --installdeps --notest --cpanfile cpanfile ."

script:
  - "make test"

notifications:
  email:
    on_failure: always
